name: "Test Workflow"

on:
  schedule: 
    - cron: "*/5 * * * *"

jobs:
  build:
    name: Build-test
    runs-on: ubuntu-latest

    steps:
      - name: Execute
        run: cd src && yarn && node index.js